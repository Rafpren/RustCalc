[package]
name = "rustcalc"
version = "1.0.1"
edition = "2024"

[dependencies]
eframe = "0.29"

[build-dependencies]
winres = "0.1"

# Garde tes options d'optimisation en bas
[profile.release]
lto = true
codegen-units = 1
panic = 'abort'
strip = true
